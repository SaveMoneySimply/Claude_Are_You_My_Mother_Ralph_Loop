# Task — hello-world

**Model:** sonnet · **Effort:** low · **Tokens estimated:** 5000 · **Attempts:** 0/3

## Steps
- [x] Step 1: Create `project/hello.sh` that echoes "Hello, Ralph!" to stdout — done when: the script exists and outputs the greeting — test: bash project/hello.sh | grep -q 'Hello, Ralph!'

## Smoke test
`bash project/hello.sh` prints "Hello, Ralph!"
