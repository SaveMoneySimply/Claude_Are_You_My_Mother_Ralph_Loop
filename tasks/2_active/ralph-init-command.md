# Task — ralph-init-command

**Model:** sonnet · **Effort:** high · **Tokens estimated:** 10000 · **Attempts:** 0/3
**Allowed files:** ralph.sh, project/ralph.sh

## Context
Add `ralph.sh init <path>` and `ralph.sh update` subcommands so Ralph can bootstrap new
projects and keep their engine files current.

`init <path>` creates a complete Ralph-ready project at the given path:
- `ralph-aymm/` subfolder containing all engine scripts copied from this repo
- `tasks/{0_backlog,1_queue,2_active,3_done}/` at project root
- `.ralph/` and `handoffs/` at project root
- `planning/` folder for blueprint, style guide, phase plans
- Template `ARCHITECTURE.md` with all required sections and placeholder content
- `CLAUDE.md` copied as-is from this repo (it's generic — travels unchanged)
- `prompt.md` copied from this repo
- Starter `CHANGELOG.md` with header comment
- `.gitignore` covering STOP, .ralph/, node_modules, .env
- `.ralph-source` file recording the absolute path of this Ralph Loop repo
- `git init && git add -A && git commit -m "init: ralph-aymm engine boilerplate"`

`update` reads `.ralph-source` from the current directory and copies the latest engine
scripts from that path into `ralph-aymm/`, then commits the update.

Engine files to copy for both init and update (from repo root into `ralph-aymm/`):
`loop.sh`, `aymm-loop.sh`, `run_agent_task.sh`, `apply_changes.sh`, `provider-config.sh`,
`provider-status.sh`, `init-firewall.sh`, `Dockerfile`, `prompt.md`

`ralph.sh` itself is NOT copied into `ralph-aymm/` — the new project gets its own
`ralph-aymm/ralph.sh` generated by init. The new `ralph-aymm/ralph.sh` is a copy of
this repo's `ralph.sh` with the comment header updated for the subfolder invocation
path (`bash ralph-aymm/ralph.sh`).

**Path patching required** — engine scripts use `SCRIPT_DIR` and `WORKDIR` to find
project state. When scripts live in `ralph-aymm/` but `tasks/` and `.ralph/` are at
the project root, these paths diverge. The init command must patch three things in
the copied scripts:

1. `loop.sh` and `aymm-loop.sh`: patch `WORKDIR=/workspace` to resolve parent dir:
   `WORKDIR="$(cd "$(dirname "$0")/.." && pwd)"`

2. `run_agent_task.sh`: uses `SCRIPT_DIR` for both engine scripts AND project state
   (`.ralph/`, `tasks/`, `ARCHITECTURE.md`). Add a self-detecting `PROJECT_ROOT` after
   the `SCRIPT_DIR` assignment:
   ```
   [ -d "$(dirname "$SCRIPT_DIR")/tasks" ] \
     && PROJECT_ROOT="$(dirname "$SCRIPT_DIR")" \
     || PROJECT_ROOT="$SCRIPT_DIR"
   ```
   Then replace all `${SCRIPT_DIR}/.ralph/`, `${SCRIPT_DIR}/tasks/`, and
   `${SCRIPT_DIR}/ARCHITECTURE.md` references with `${PROJECT_ROOT}/...`
   Leave `${SCRIPT_DIR}/apply_changes.sh`, `${SCRIPT_DIR}/provider-config.sh` etc.
   pointing to SCRIPT_DIR (they're engine scripts, correctly in ralph-aymm/).

3. `aymm-loop.sh`: calls `run_agent_task.sh` via `bash "${WORKDIR}/run_agent_task.sh"`.
   In the patched copy, WORKDIR is the project root so this needs to become
   `bash "${WORKDIR}/ralph-aymm/run_agent_task.sh"`. Patch with sed after copy.

## Template ARCHITECTURE.md content
Should include all standard sections with clear placeholder text:
- Project name, one-line description
- ## Stack (language, framework, runtime, package manager)
- ## Test Command (the command that must pass before any task closes)
- ## Directory Structure (key folders and what lives in each)
- ## Ralph settings (autonomy: low, loop version, provider count)
- ## Firewall additions (any extra domains beyond the default allowlist)
- ## Doc map (README, CLAUDE.md, CHANGELOG, planning/, tasks/)

## Steps
- [x] Add `ralph_init()` function to ralph.sh: creates target directory, copies engine files into `ralph-aymm/`, applies all three path patches (WORKDIR in loop scripts, PROJECT_ROOT auto-detection in run_agent_task.sh, run_agent_task.sh path in aymm-loop.sh), writes template ARCHITECTURE.md with all placeholder sections, copies CLAUDE.md and prompt.md, writes starter CHANGELOG.md and .gitignore, writes `.ralph-source` with this repo's absolute path, runs git init and initial commit. Wire into the MODE case statement as `init` — done when: `ralph.sh init` mode exists with all three patches applied — test: bash -n ralph.sh && grep -q 'ralph_init\|init)' ralph.sh -- files: ralph.sh:1-15, ralph.sh:59-100
- [ ] Add `ralph_update()` function to ralph.sh: reads `.ralph-source`, validates path exists, copies latest engine scripts into `ralph-aymm/`, re-applies all three path patches, commits the update. Wire into MODE case as `update`. Mirror both functions to project/ralph.sh — done when: both modes exist in both files — test: bash -n ralph.sh && bash -n project/ralph.sh && grep -q 'ralph_update\|update)' ralph.sh && grep -q 'ralph_update\|update)' project/ralph.sh -- files: ralph.sh:1-15, ralph.sh:59-100, project/ralph.sh:1-15

## Smoke test
Run `bash ralph.sh init /tmp/test-ralph-project` and verify:
- `/tmp/test-ralph-project/ralph-aymm/loop.sh` exists and `bash -n` passes
- `grep 'WORKDIR=/workspace' /tmp/test-ralph-project/ralph-aymm/loop.sh` returns nothing
- `grep 'PROJECT_ROOT' /tmp/test-ralph-project/ralph-aymm/run_agent_task.sh` returns a match
- `grep 'ralph-aymm/run_agent_task.sh' /tmp/test-ralph-project/ralph-aymm/aymm-loop.sh` returns a match
- `/tmp/test-ralph-project/ARCHITECTURE.md` exists with all placeholder sections
- `/tmp/test-ralph-project/tasks/1_queue/` and `planning/` exist
- `/tmp/test-ralph-project/.ralph-source` contains this repo's absolute path
- `git -C /tmp/test-ralph-project log --oneline` shows the initial commit
