# Hello Ralph — Example Project

## What This Is
A minimal shell project used to validate the Ralph fork model end-to-end.
Ralph's job: create `hello.sh` that prints a greeting.

## Stack
- Shell: bash

## Test Command
```bash
bash hello.sh | grep -q "Hello, Ralph!"
```

## Key Files
- `hello.sh` — main greeting script (created by Ralph)

## Ralph Settings
autonomy: low
